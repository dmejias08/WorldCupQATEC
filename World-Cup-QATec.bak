#lang racket


(define (rand num cycles)
  (cond
    ((zero? cycles)
     '())
    (else
     (cons (list (random num)) (rand num (- cycles 1))))))

(rand 2 10)

;; First Gen

(define (team defenders midFilders forwards goalKeeper)
  (cond
    (()
     )
    ))